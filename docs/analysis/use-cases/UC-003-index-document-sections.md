# UC-003 — Index Document Sections

//TODO - Elicit and write this use case (WVR-89): the tool that produces
`.index/<slug>.sections.yaml` for a document (section/code-block id, title,
type, start_line, end_line).

# UC-006 — Extract Document Content

//TODO - Elicit and write this use case (WVR-92): the tool that extracts
just the content of a given document or a specific section/code-block (by
id, using `.index/<slug>.sections.yaml`'s line ranges) rather than
requiring a full-document read.

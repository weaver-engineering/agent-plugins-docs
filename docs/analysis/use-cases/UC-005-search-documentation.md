# UC-005 — Search Documentation

//TODO - Elicit and write this use case (WVR-91): the tool that searches
`.index/` word/section data across a doc, directory, or whole docs repo for
a query string and returns ranked doc/section results.
